#include <iostream>
using namespace std;

int main() {
    char a;
    bool is_a = false;
    while (cin >> a and (a != 'a' or a != 0)) {
        if (a == 'a') is_a = true;
        
    }
    if (is_a) cout << "yes" << endl;
    else cout << "no" << endl;
}
